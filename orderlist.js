/**
 * 구매 내역(Order List) 페이지 전용 스크립트
 */

document.addEventListener("DOMContentLoaded", () => {
    // 필터링이나 초기화 로직 필요 시 작성
});

// 배송 조회 (임시 알림)
function trackDelivery() {
    alert("배송 조회 팝업을 띄웁니다. (택배사 연동 필요)");
}

// 리뷰 쓰기 (임시 이동)
function writeReview(orderId) {
    // window.location.href = '/user/review/write/' + orderId;
    alert("리뷰 작성 페이지로 이동합니다.");
}